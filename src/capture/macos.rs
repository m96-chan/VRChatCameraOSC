//! macOS webcam backend (AVFoundation via `nokhwa`).
//!
//! Filled in by the camera-capture work (issue #2). Kept minimal so the crate
//! compiles with the `camera` feature enabled.

// Intentionally empty for now — the nokhwa-backed `MacosCamera` lands with
// issue #2. Placeholder module so `mod macos;` resolves under the feature gate.
